:css
  .avatar {
    position: relative;
    left: 360px;
  }
  .name {
    position: relative;
    left: 487px;
    top: 21px;
    font-size: 230%;
  }

  .residence {
    position: relative;
    left: 489px;
    top: -64px;
    font-size: 110%;
  }
  .mobile {
    position: relative;
    left: 489px;
    top: -64px;
    font-size: 110%;
    color: grey;

  }
  .email {
    position: relative;
    left: 489px;
    top: -64px;
    font-size: 110%;
    color: grey;
  }



- if current_user.admin
  #{@user.first_name} #{@user.last_name}
  - if @user.avatar_url.to_s != ''
    %p
      %img{:src => @user.avatar_url.to_s, :width => "120",}/
  /- if @user.gender
  /  .ui.segment
  /    Gender: #{@user.gender}
  E-mail: #{@user.email}
  /- if @user.date_of_birth
  /  .ui.segment
  /    Date of Birth: #{@user.date_of_birth.strftime("%m/%d/%Y")}
  - if @user.country_of_residence
    .residence
      From #{@user.city_of_residence}, #{@user.country_of_residence}
  -else
    .residence
      From #{@user.city_of_residence}
  - if @user.contact_phone
    Mobile: #{@user.contact_phone}
  /- if @user.education
  /  .ui.segment
  /    Educational Interests: #{@user.education}
  /-if @var || @user.id == current_user.id
  /  - if @user.work
  /    .ui.segment
  /      Professional Interests: #{@user.work}
  /-if @var || @user.id == current_user.id
  /  - if @user.interests
  /    .ui.segment
  /      Hobbies: #{@user.interests}
  /-if @var || @user.id == current_user.id
  /  - if @user.capabilities
  /    .ui.segment
  /      Capabilities: #{@user.capabilities}

- else
  -if @var || @user.id == current_user.id
    .name
      #{@user.first_name} #{@user.last_name}
  -else
    .name
      #{@user.first_name}
  - if @user.avatar_url.to_s != ''
    %p
      %img.avatar{:src => @user.avatar_url.to_s, :width => "120"}/
  /- if @user.gender
  /  .ui.segment
  /    Gender: #{@user.gender}
  /- if @user.date_of_birth
  /  .ui.segment
  /    Date of Birth: #{@user.date_of_birth.strftime("%m/%d/%Y")}
  - if @user.country_of_residence != ''
    .residence
      From #{@user.city_of_residence}, #{@user.country_of_residence}
  -else
    .residence
      From #{@user.city_of_residence}
  -if @var || @user.id == current_user.id
    .mobile
      - if @user.contact_phone != ''
        Mobile: #{@user.contact_phone}
  -if @var || @user.id == current_user.id
    .email
      E-mail: #{@user.email}
  #{@numadventures}
  #{@numactions}
  /- if @user.education
  /  .ui.segment
  /    Educational Interests: #{@user.education}
  /-if @var || @user.id == current_user.id
  /  - if @user.work
  /    .ui.segment
  /      Professional Interests: #{@user.work}
  /-if @var || @user.id == current_user.id
  /  - if @user.interests
  /    .ui.segment
  /      Hobbies: #{@user.interests}
  /-if @var || @user.id == current_user.id
  /  - if @user.capabilities
  /    .ui.segment
  /      Capabilities: #{@user.capabilities}






