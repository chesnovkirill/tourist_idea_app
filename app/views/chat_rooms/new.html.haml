%h1 Choose a Name for your Adventure
= form_for @chat_room do |f|
  .form-group
    = f.label :title
    = f.text_field :title, autofocus: true, class: 'form-control'
  = f.submit "Add!", class: 'btn btn-primary'

