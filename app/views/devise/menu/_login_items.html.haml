- if user_signed_in?
  %li
    = link_to '<i class="big sign out icon"></i>'.html_safe, destroy_user_session_path, method: :delete
- else
  %li
    = link_to '<i class="add user icon"></i>'.html_safe, new_user_session_path

